{
  "name": "image-gen-server",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server for unified image generation across multiple providers",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "bfl-api": "*",
    "google-genai-api": "*",
    "ideogram-api": "*",
    "openai-image-api": "*",
    "stability-ai-api": "*"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0"
  }
}
